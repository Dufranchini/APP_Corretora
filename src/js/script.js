document.getElementById('loadData').addEventListener('click', () => {
    alert('Conectando ao servidor e carregando dados do GPS...');
    // Aqui futuramente será feita a requisição ao servidor PHP
});
